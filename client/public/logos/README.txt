Drop company logo PNGs here, named to match each room's `slug` in
client/src/pages/Landing.jsx (e.g. tcs.png, amazon.png, google.png).

If a logo file is missing, the room card automatically falls back to a
black tile with the company's initial letter, so nothing breaks.
